class Solution {
public:
	int countPrimes(int n) {
		vector<bool> v(n, true);

		for (long long i = 2; i < n; ++i) {
			if (v[i] == true) {
				for (long long j = i * i; j < n; j += i) v[j] = false;
			}
		}
		long long ct = 0;
		for (int i = 2; i < n; ++i) if (v[i]) ct++;
		return ct;
	}
};
